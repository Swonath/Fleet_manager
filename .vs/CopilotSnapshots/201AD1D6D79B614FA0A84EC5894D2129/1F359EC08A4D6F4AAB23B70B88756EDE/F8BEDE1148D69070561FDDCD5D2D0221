<Window x:Class="FLEET_MANAGER.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fleet Manager - Connexion" Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="350">
            <!-- Logo/Titre -->
            <TextBlock Text="FLEET MANAGER" 
                       FontSize="32" 
                       FontWeight="Bold" 
                       Foreground="#1e3a8a"
                       TextAlignment="Center"
                       Margin="0,0,0,10"/>
            
            <TextBlock Text="Gestion de parc automobile" 
                       FontSize="14" 
                       Foreground="#64748b"
                       TextAlignment="Center"
                       Margin="0,0,0,30"/>

            <!-- Champ Nom d'utilisateur -->
            <TextBlock Text="Nom d'utilisateur" FontSize="12" Foreground="#334155" Margin="0,0,0,5"/>
            <TextBox x:Name="UsernameTextBox"
                     Text="{Binding NomUtilisateur, UpdateSourceTrigger=PropertyChanged}"
                     Height="40"
                     Padding="10"
                     FontSize="14"
                     Margin="0,0,0,15"
                     Background="White"
                     BorderBrush="#cbd5e1"
                     BorderThickness="1"/>

            <!-- Champ Mot de passe -->
            <TextBlock Text="Mot de passe" FontSize="12" Foreground="#334155" Margin="0,0,0,5"/>
            <PasswordBox x:Name="PasswordBox"
                         Height="40"
                         Padding="10"
                         FontSize="14"
                         Margin="0,0,0,15"
                         Background="White"
                         BorderBrush="#cbd5e1"
                         BorderThickness="1"/>

            <!-- Message d'erreur/succès -->
            <TextBlock Text="{Binding Message}"
                       Foreground="#dc2626"
                       TextWrapping="Wrap"
                       Margin="0,0,0,15"
                       FontSize="12"/>

            <!-- Bouton Connexion -->
            <Button Command="{Binding LoginCommand}"
                    Content="{Binding IsLoading, StringFormat='Connexion en cours...', FallbackValue='Connexion'}"
                    Height="45"
                    FontSize="14"
                    FontWeight="Bold"
                    Background="#1e3a8a"
                    Foreground="White"
                    Cursor="Hand"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InvertBoolConverter}, FallbackValue=True}"/>

            <!-- Lien de création de compte -->
            <TextBlock TextAlignment="Center" Margin="0,20,0,0">
                <Hyperlink NavigateUri="#" Foreground="#3b82f6">
                    Vous n'avez pas de compte ? Demandez un accès administrateur
                </Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
